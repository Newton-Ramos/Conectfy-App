/** Identidade visual Conectfy — teal profundo → teal claro (referência image_14). */
export const BRAND_TEAL_DEEP = '#1b6b65';
export const BRAND_TEAL_MID = '#2a9d8f';
export const BRAND_TEAL_LIGHT = '#48a79f';

export const BRAND_GRADIENT_COLORS = [BRAND_TEAL_DEEP, BRAND_TEAL_MID, BRAND_TEAL_LIGHT] as const;

/** Compatível com botões/links já existentes que usavam verde anterior. */
export const BRAND_ACCENT = '#2c9a81';

/** Fundos claros de lista (chat, conversas, contatos). */
export const APP_SURFACE_BG = '#e9edef';

/** Gradiente hero / headers alinhados à welcome e login. */
export const AUTH_GRADIENT_COLORS = ['#0F3D3E', '#134e4a', '#1a8a8a'] as const;

export const SLOGAN_UPPER = 'SUA PRODUTIVIDADE, CONECTADA';

/** Logo 3D metal double C (PNG no bundle). */
export const LOGO_IMAGE = require('@/assets/images/Conectfy Logo 1024 x 1024.png');
